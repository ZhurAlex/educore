<h1><%= @school_class.name %></h1>

<h2><%= t("test_assignments.show.pick_yourself") %></h2>
<ul>
  <% @students.each do |student| %>
    <li><%= link_to student.name_with_initial, student_history_login_path(@school_class, student) %></li>
  <% end %>
</ul>
